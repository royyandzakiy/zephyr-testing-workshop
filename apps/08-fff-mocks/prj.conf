CONFIG_GPIO=y
CONFIG_PRINTK=y

# Leave the sensor stack off by default so a plain `west build -b
# native_sim/native` picks src/sensor_port_sim.c and runs with no I2C at all.
# Turn these on (and add a bme280 node) for a real board:
# CONFIG_I2C=y
# CONFIG_SENSOR=y
# CONFIG_BME280=y
