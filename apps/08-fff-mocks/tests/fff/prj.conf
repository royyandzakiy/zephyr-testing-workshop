CONFIG_ZTEST=y
CONFIG_PRINTK=y

# No CONFIG_GPIO. Nothing under test here touches a driver, so nothing here
# enables one.
#
# <zephyr/fff.h> needs no symbol of its own: ztest already puts it on the
# include path.
