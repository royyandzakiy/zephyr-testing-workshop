tests:
  app08.climate.fff:
    # No devicetree node is bound and no driver is linked, so this would run
    # on anything. native_sim is simply the fastest place to find out.
    platform_allow:
      - native_sim
    tags:
      - climate
      - unit
      - fff
