tests:
  app08.dispenser.fff:
    # No devicetree node is bound and no driver is linked, so this would run
    # anywhere. native_sim is simply the fastest place to find out.
    platform_allow:
      - native_sim
    tags:
      - feeder
      - unit
      - fff
