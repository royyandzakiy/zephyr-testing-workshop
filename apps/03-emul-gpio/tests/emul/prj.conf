CONFIG_ZTEST=y
CONFIG_PRINTK=y
CONFIG_GPIO=y

# The emulated controller. Also implied by the `zephyr,gpio-emul` node in
# app.overlay, but spelled out here so the suite is self-describing.
CONFIG_GPIO_EMUL=y
