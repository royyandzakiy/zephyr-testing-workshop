# Nothing to configure. printk() is on by default; there is no driver to
# enable and no devicetree node to bind. That is the point of this app --
# if this builds and runs, your toolchain is fine.
CONFIG_PRINTK=y
